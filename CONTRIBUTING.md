# Contributing

# Setup Development

Environment setup:

```
$ python3 -m venv venv
$ source venv/bin/activate
$ pip3 install -r dev-requirements.txt
$ pip3 install -r test-requirements.txt
```

Run tests:
```
$ pytest
```
