# Contributing

# Setup Development

Environment setup:

```
$ python3 -m venv venv
$ source venv/bin/activate
$ pip3 install -r requirements_dev.txt
$ pip3 install -r requirements_test.txt
```

Run tests:
```
$ pytest
```
